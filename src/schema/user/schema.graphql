extend type Query {
	product(id: ID!): Product
	reviews: [Review!]!
}

type Product {
	id: ID!
	name: String!
	reviews: [Review!]!
	date: DateTime!
}

type Review {
	id: ID!
	product: Product!
	body: String!
}
